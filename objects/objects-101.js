let person = {
  name: 'Ajie',
  age: 21,
  location: 'Indonesia'
};

console.log(`${person.name} is ${person.age} and live in ${person.location}`);

person.age = person.age + 1;

console.log(`${person.name} is ${person.age} and live in ${person.location}`);
